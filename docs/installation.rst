============
Installation
============

At the command line::

    $ easy_install djangocms-hero-slider

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-hero-slider
    $ pip install djangocms-hero-slider
